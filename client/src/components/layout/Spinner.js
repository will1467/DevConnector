import React, { Fragment } from 'react';

const Spinner = props => {
    return (
        <Fragment>
            <i className="fa fa-spinner fa-spin fa-4x"></i>
        </Fragment>
    )
}

export default Spinner;
